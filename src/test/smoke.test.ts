import { describe, it, expect } from 'vitest'

// Proves the test harness runs. Replaced by real core/* tests as they land.
describe('harness', () => {
  it('runs vitest', () => {
    expect(1 + 1).toBe(2)
  })
})
